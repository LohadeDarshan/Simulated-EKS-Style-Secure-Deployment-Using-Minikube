apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: auth-service-monitor
  namespace: app
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: auth-service
  endpoints:
  - port: http
    interval: 30s